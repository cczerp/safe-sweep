# RPC URL (should support WebSocket for mempool monitoring)
# For example: wss://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY
RPC_URL=wss://your-rpc-url-here

# Gnosis Safe address to protect
SAFE_ADDRESS=0x1234567890123456789012345678901234567890

# Module address where tokens should be swept to
MODULE_ADDRESS=0x0987654321098765432109876543210987654321

# Private key of a Safe owner (used to sign sweep transactions)
OWNER_PRIVATE_KEY=0xYourPrivateKeyHere

# Comma-separated list of addresses authorized to move tokens
# These addresses will NOT trigger the emergency sweep
AUTHORIZED_ADDRESSES=0xAuthorizedAddress1,0xAuthorizedAddress2

# Optional: Check interval in milliseconds (default: 1000)
CHECK_INTERVAL_MS=1000

# Optional: Gas multiplier for sweep transactions (default: 1.5)
# This ensures sweep transaction is faster than malicious one
GAS_MULTIPLIER=1.5

# Optional: Maximum gas price in wei (prevents overpaying)
# MAX_GAS_PRICE=100000000000
